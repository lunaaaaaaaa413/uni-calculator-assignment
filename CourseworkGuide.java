
//CourseworkGuide – provides documentation and suggested extensions for the Calculator coursework.
/**
 *  A super light calculator.
 * 
 * <p> It uses MVC (Model-View-Controller) structure. And a {@link Main} class, which runs as a javafx application.
 * View includes four action buttons, but only the add(+) button works.
 * 
 * <p> There is also a simple {@link Calculator} class, which does the calculation add(+), sub(-), mul(×),div(/).
 * It also contains unit test class {@link CalculatorTest} which test Calculator,
 * you need to write your testing code if you want to do Junit testing. 
 * 
 * <p> It also include a .css file to stylize the interface.
 * 
 * <h2>Project features</h2>
 * <p>Your code includes the following features which will all gain marks if you use them in 
 * your project code:
 * 
 * <ul>
 * <li>control statements (loops, if etc)</li> done
 * <li>using classes, objects and methods</li> done
 * <li>inheritance</li>
 * <li>encapsulation and protected variables</li> done
 * <li>using collection classes (TreeMap)</li>
 * <li>JUnit tests</li>
 * <li>Javadoc comments and compiled documentation</li> done i think
 * <li>code comments</li> done
 * <li>using file IO for persistent storage</li> done
 * </ul>
 
 * 
 * @author Shine
 * @version 2025-CI401-Calcualtor
 */
public class CourseworkGuide{
    // suggestions for new features:
    //1. make all buttons work.
    //2. Adding effects like colour,sound.
    //3. add more action buttons(clear, history)
}
